---
import { Image } from "astro:assets";



const { title, items, svgSrc, titlebg } = Astro.props ;
---
<!-- 

  <div class="text-center">
    <h2 class="text-xl font-bold text-yellow-700">{title}</h2>
    <hr class="mt-2 border-t-2 border-gray-500">
  </div>

  <div class="flex flex-row items-center justify-evenly">
    <div class="mb-4">
        <svg class="w-16 h-16 text-yellow-700" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 1.5v21m9-13.5H3m0 6h18"></path></svg>
    </div>
    <ul class="text-gray-700 list-disc list-inside">
        {items.map((item) => (
          <li>{item}</li>
        ))}
    </ul>
  </div> -->

  <div class={`text-center ${titlebg} rounded-t-badge`}>
    <h2 class="text-xl font-bold text-white">{title}</h2>
    <hr class="mt-2 border-t-2 border-gray-500">
  </div>

  <div class="grid grid-cols-2 gap-1 p-2 ">
    <ul class="text-gray-800 list-disc list-inside leading-tight inside-body-text">
      {items.map((item) => (
        <li class="whitespace-nowrap  ">{item}</li>
      ))}
    </ul>
    <div class="m-4 self-center justify-self-end">
        <Image class="w-16 h-16" src={svgSrc} alt="{title} icon" class="aspect-square object-cover"/>
    </div>
 
  </div>